!!! 5
%html
  %head
    %title= t("app.title")
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  %body
    %header.navbar.navbar-static-top.navbar-inverse
      %nav.container
        .navbar-header
          = link_to t("app.title"), root_path, class: "navbar-brand"
        %ul.nav.navbar-nav.navbar-right
          - if user_signed_in?
            %li= link_to t("title.register_plan"), new_users_plan_path
            %li= link_to t("title.manage_plan"), users_plans_path
            %li= link_to Profile.model_name.human, users_profile_path
            %li= link_to t("title.user_info"), edit_user_registration_path
            %li= link_to t("title.edit_password"), users_password_path
            %li= link_to t("title.sign_out"), destroy_user_session_path, method: :delete
          - else
            %li= link_to t("title.sign_up"), new_user_registration_path
            %li= link_to t("title.sign_in"), new_user_session_path
    .container
      - flash.each do |message_type, message|
        = content_tag :div, message, class: "alert alert-#{message_type}"
      = yield
