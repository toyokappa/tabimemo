%ul.plan-status-menu
  %li= link_to t(".least_plans"), user_profile_path(user.name), remote: true, class: "active"
  %li= link_to t(".liked_plans"), user_liked_plans_path(user.name), remote: true
  %li= link_to t(".trophy"), user_trophy_path(user.name)

