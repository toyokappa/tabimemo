%h2= t(".title")
- if @plans.present?
  %ul
    - @plans.each do |plan|
      %li
        %h2= link_to plan.name, users_plan_path(plan)
        %p= plan.description
- else
  = t("view.no_plans")
= paginate @plans
