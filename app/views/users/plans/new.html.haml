%h2= t(".title")
= simple_form_for @plan, url: users_plans_path do |f|
  = f.input :name
  = f.input :description, input_html: { rows:5 }
  = f.button :submit, t("form.register_plan"), class: "btn-primary"
